#!maicro
#---
# title: Query Weather Readings
# description: Reads back stored weather ingestion rows for validation and exploration.
# difficulty: beginner
# estimated_time: 1m
# tags:
#   - query
#   - cron
#   - database
#---

###> STEP 1 - LIST RECENT READINGS
#{
# Shows recently inserted weather rows from the cron worker.
#}
query Step1_ListWeatherReadings {
  weather_readings(page: { page: 1, items: 100 }) {
    id
    location
    temperature
    timestamp
  }
}
